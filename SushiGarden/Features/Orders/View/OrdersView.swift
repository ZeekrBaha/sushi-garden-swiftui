// TEMP-STUB: replaced in Task 4.2
import SwiftUI

struct OrdersView: View {
    let deps: Dependencies

    var body: some View {
        Text(Strings.Tabs.orders).foregroundStyle(AppColor.textPrimary)
    }
}
