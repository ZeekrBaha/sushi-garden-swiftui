// TEMP-STUB: replaced in Task 2.4
import SwiftUI

struct CatalogView: View {
    let deps: Dependencies

    var body: some View {
        Text(Strings.Tabs.catalog).foregroundStyle(AppColor.textPrimary)
    }
}
